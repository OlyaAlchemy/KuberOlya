apiVersion: v1
kind: Service
metadata:
  name: {{ include "wordp.fullname" . }}
  labels:
    {{- include "wordp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wordp.selectorLabels" . | nindent 4 }}
